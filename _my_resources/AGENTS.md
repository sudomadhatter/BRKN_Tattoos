# `_my_resources/` — LOCAL LAW (Daniel's personal area — PROTECTED)

You are in Daniel's **personal area** — brainstorming, PRPs, agentic-loop notes, diagrams kept for his
own use. Default posture: **READ-ONLY**, read only what a task or a routing row points you at. What's
here → `README.md`.

## The law
- **NEVER create, edit, move, or delete anything here on your own.** Touch a file only when Daniel
  explicitly directs you to that file, in the moment. (An approval elsewhere doesn't carry in here.)
- **Not auto-context:** never reference or pull context from here unless Daniel links the specific
  document — content may be stale and is NOT authoritative; cross-check against live project files.
- **ONE standing exception:** `/1_update-maps` refreshes the `## Open Work` file-list inside
  `open_tasks/todo_list.md` — only that list; his `## Todo list` prose and the task files stay his.
- `open_tasks/todo_list.md` — Daniel's "what's next" queue. Surfaced READ-ONLY on "pick up" and on
  "what's next / open tasks" asks.
- The agent's *reference* material lives elsewhere: `docs/` (verified shelf), `_bmad/` + `_bmad-output/`
  (BMAD state), `_artifacts/` + home-base `_artifacts/clean-bmad-workspace/` (session history). Daniel
  promotes verified files out of here himself.
