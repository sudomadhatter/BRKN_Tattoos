# `_my_resources/` — Daniel's personal area

These are **Daniel's personal documents** — brainstorming, PRPs, agentic-loop notes, and diagrams kept for
his own use. What's in here right now:
- `Agentic_Loops/` — personal notes on autonomous dev/QA loops.
- `Docs/` — personal workspace/method guides.
- `Visual_Diagrams/` — personal overview diagrams.

## ⚠️ Read this before touching anything here
- **Personal use only.** This is a staging/brain-dump area — content here **may be stale and is NOT
  authoritative.** Always check it against the live project files before trusting it.
- **Agents must NOT edit any file in `_my_resources/` unless Daniel explicitly says so.**
  - **One standing exception:** `/1_update-maps` may refresh the **`## Open Work` file-list inside
    `open_tasks/todo_list.md`** so it mirrors the plan/PRP `.md` files dropped in `open_tasks/`. It writes
    **only** that manifest — never the `## Todo list` notes, never the task files, never anything else here.
- **Agents must NOT reference or pull context from anything here unless Daniel links the specific document.**
  It is not auto-context.

## Where the agent's *reference* material lives instead
- **`docs/`** is the agent's verified reference shelf (repo-map, tech-stack, skills-registry, and anything
  Daniel has confirmed is current).
- **`_bmad/`** holds the BMAD documents that can be referenced.
- **Home-base `_artifacts/clean-bmad-workspace/`** holds the session history (plans, walkthroughs, chats) the
  agent can dig into when it needs deeper context.

Daniel will **slowly promote** files from here into `docs/` once he has verified they're up to date. Until a
doc has been promoted (or linked by Daniel), treat it as personal and off-limits.
