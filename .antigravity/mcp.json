{
  "mcpServers": {
    "firebase": {
      "command": "npx.cmd",
      "args": [
        "-y",
        "firebase-tools@latest",
        "mcp"
      ],
      "env": {
        "GCLOUD_PROJECT": "{{PROJECT_NAME}}",
        "FIRESTORE_DATABASE": "(default)"
      }
    }
  }
}
